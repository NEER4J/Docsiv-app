import { notFound } from 'next/navigation';

/**
 * Public document page — /p/{documentId}
 * Placeholder for future public document feature.
 */
export default async function PublicDocumentPage() {
  notFound();
}
